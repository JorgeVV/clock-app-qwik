diff --git a/node_modules/@builder.io/qwik-city/middleware/netlify-edge/index.mjs b/node_modules/@builder.io/qwik-city/middleware/netlify-edge/index.mjs
index 9e5dc0f..2b4175e 100644
--- a/node_modules/@builder.io/qwik-city/middleware/netlify-edge/index.mjs
+++ b/node_modules/@builder.io/qwik-city/middleware/netlify-edge/index.mjs
@@ -561,7 +561,7 @@ async function requestHandler(requestCtx, render, platform, opts) {
 
 // packages/qwik-city/middleware/netlify-edge/index.ts
 function qwikCity(render, opts) {
-  async function onRequest(request, { next }) {
+  async function onRequest(request, { next, ...netlifyContext}) {
     try {
       const requestCtx = {
         url: new URL(request.url),
@@ -595,13 +595,13 @@ function qwikCity(render, opts) {
           });
         }
       };
-      const handledResponse = await requestHandler(requestCtx, render, {}, opts);
+      const handledResponse = await requestHandler(requestCtx, render, netlifyContext, opts);
       if (handledResponse) {
         return handledResponse;
       }
       const nextResponse = await next();
       if (nextResponse.status === 404) {
-        const handledResponse2 = await requestHandler(requestCtx, render, {}, opts);
+        const handledResponse2 = await requestHandler(requestCtx, render, netlifyContext, opts);
         if (handledResponse2) {
           return handledResponse2;
         }
